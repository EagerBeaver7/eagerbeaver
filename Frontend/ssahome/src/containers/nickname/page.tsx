'use client';

import { Suspense } from 'react';
import dynamic from 'next/dynamic';

const NickNamePage = () => {
  return (
    <Suspense>
      닉네임 설정
    </Suspense>
  );
};

export default NickNamePage;
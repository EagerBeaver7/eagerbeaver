'use client';

import { Suspense } from 'react';
import dynamic from 'next/dynamic';

const ResultPage = () => {
  return (
    <Suspense>
      결과 페이지
    </Suspense>
  );
};

export default ResultPage;